Django>=5.1,<5.2
djangorestframework>=3.15,<4.0
django-filter>=24.0
django-cors-headers>=4.4
django-environ>=0.11
whitenoise>=6.7
PyMySQL>=1.1
gunicorn>=22.0

# Only needed if you switch DATABASE_URL to postgres:// (e.g. on a paid
# PythonAnywhere tier). Free-tier PythonAnywhere is MySQL-only, and PyMySQL
# above (a pure-Python driver, no system libraries required) covers that.
# psycopg2-binary>=2.9
